# Analyze GitHub Actions workflows and the maintained Python sources.
# .github/codeql-config.yml carries only a paths-ignore for the
# Jinja-templated cookiecutter sources, which CodeQL cannot parse.
# With no paths allow-list, default Python extraction covers everything
# else — including packages/, which was previously missing from analysis —
# and the actions extractor's default workflow-file include is preserved
# (a user paths allow-list would override it and extract zero workflows).
#
name: "CodeQL"

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
      - "v*"
    paths:
      - '.github/**'
      - '**.py'  # Any Python file
  pull_request:
    paths:
      - '.github/**'
      - '**.py'  # Any Python file
  schedule:
    - cron: '37 10 * * 5'

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions: {}

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      security-events: write # create security reports for the repo

    strategy:
      fail-fast: false
      matrix:
        language: [ 'actions', 'python' ]
        # CodeQL supports Actions plus compiled and interpreted source languages.
        # Learn more about CodeQL language support at https://aka.ms/codeql-docs/language-support

    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      with:
        persist-credentials: false

    # Initializes the CodeQL tools for scanning.
    - uses: github/codeql-action/init@d1ba80a13dd99fba24a470575428917156a28b43 # v4.37.5
      with:
        config-file: .github/codeql-config.yml
        languages: ${{ matrix.language }}
        # If you wish to specify custom queries, you can do so here or in a config file.
        # By default, queries listed here will override any specified in a config file.
        # Prefix the list here with "+" to use these queries and those in the config file.

        # Details on CodeQL's query packs refer to : https://docs.github.com/en/code-security/code-scanning/automatically-scanning-your-code-for-vulnerabilities-and-errors/configuring-code-scanning#using-queries-in-ql-packs
        # queries: security-extended,security-and-quality


    # Autobuild attempts to build any compiled languages  (C/C++, C#, or Java).
    # If this step fails, then you should remove it and run the build manually (see below)
    - uses: github/codeql-action/autobuild@d1ba80a13dd99fba24a470575428917156a28b43 # v4.37.5

    # ℹ️ Command-line programs to run using the OS shell.
    # 📚 See https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idstepsrun

    #   If the Autobuild fails above, remove it and uncomment the following three lines.
    #   modify them (or add more) to build your code if your project, please refer to the EXAMPLE below for guidance.

    # - run: |
    #   echo "Run, Build Application using script"
    #   ./location_of_script_within_repo/buildscript.sh

    - uses: github/codeql-action/analyze@d1ba80a13dd99fba24a470575428917156a28b43 # v4.37.5
